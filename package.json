{
  "name": "untitled-space-game",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:docs": "bun scripts/build-docs.ts",
    "build:map": "bun scripts/build-map.ts",
    "build:landing": "bun scripts/build-landing.ts",
    "build:font-report": "bun scripts/build-font-report.ts",
    "build:all": "npm run build && npm run build:docs && npm run build:map && npm run build:landing && npm run build:font-report",
    "preview": "vite preview",
    "test": "vitest run",
    "terminal": "bun terminal.ts"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^3.14.2",
    "jsdom": "^29.1.1",
    "marked": "^18.0.3",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
